import { BarreDirective } from './barre.directive';

describe('BarreDirective', () => {
  it('should create an instance', () => {
    const directive = new BarreDirective();
    expect(directive).toBeTruthy();
  });
});
